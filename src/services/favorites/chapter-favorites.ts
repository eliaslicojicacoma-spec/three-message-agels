export {
  getBibleFavorites,
  getBibleHistory,
  isBibleFavorite,
  saveBibleHistory,
  toggleBibleFavorite,
} from "@/lib/storage/bible-progress";

export type {
  BibleFavoriteItem,
  BibleHistoryItem,
} from "@/lib/storage/bible-progress";
